import logoImage from '../assets/images/logo.png'

export const siteConfig = {
  preloader: {
    type: 'text', // Change to "logo" when a logo image is supplied.
    text: 'EDYN',
    logoSrc: logoImage,
    minimumDuration: 1200,
  },
}
