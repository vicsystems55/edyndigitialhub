# URL of the deployed Node API. Leave blank when frontend and API share an origin.
VITE_API_URL=http://localhost:5001

# Development only. Set false once the backend admin login endpoint is connected.
VITE_ADMIN_DEMO_MODE=false
